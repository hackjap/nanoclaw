---
phase: 02-ai-conversation-agent
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - groups/global/CLAUDE.md
autonomous: false
requirements: [CONV-01, CONV-02]
must_haves:
  truths:
    - "When user @mentions bot with a work request, the agent asks clarifying questions in the thread"
    - "Agent infers fields from initial message before asking (inference-first per D-01)"
    - "Agent collects title, description, and issueType through conversation in Korean"
    - "When all 3 fields are collected, agent calls submit_jira_draft automatically"
  artifacts:
    - path: "groups/global/CLAUDE.md"
      provides: "Jira draft collection instructions for the agent"
      contains: "submit_jira_draft"
  key_links:
    - from: "groups/global/CLAUDE.md"
      to: "container/agent-runner/src/ipc-mcp-stdio.ts"
      via: "Agent follows CLAUDE.md instructions to call submit_jira_draft MCP tool"
      pattern: "submit_jira_draft"
---

<objective>
Add Jira issue collection instructions to the agent's CLAUDE.md so it conducts focused multi-turn conversations to gather issue details and submits drafts via the MCP tool.

Purpose: CONV-01 and CONV-02 require the agent to conduct inference-first Korean conversations to collect Jira issue fields. The agent behavior is entirely driven by CLAUDE.md instructions — no code changes needed beyond Plan 01's infrastructure.
Output: Updated groups/global/CLAUDE.md with Jira collection section, verified end-to-end in Slack.
</objective>

<execution_context>
@/Users/spjang/workspace/nanoclaw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/spjang/workspace/nanoclaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai-conversation-agent/02-CONTEXT.md
@.planning/phases/02-ai-conversation-agent/02-RESEARCH.md
@.planning/phases/02-ai-conversation-agent/02-01-SUMMARY.md

<interfaces>
<!-- The MCP tool the agent will call, created in Plan 01 -->

From container/agent-runner/src/ipc-mcp-stdio.ts (after Plan 01):
```typescript
server.tool(
  'submit_jira_draft',
  'Submit a Jira issue draft after collecting all required information from the user.',
  {
    title: z.string().describe('Issue title — concise one-line summary'),
    description: z.string().describe('Issue description — detailed content'),
    issueType: z.enum(['Bug', 'Task', 'Story']).describe('Issue type'),
  },
  async (args) => { ... }
);
```

From groups/global/CLAUDE.md (current structure):
- Lines 1-116: Standard agent instructions (identity, workspace, memory, formatting, task scripts)
- Lines 117-125: Custom section with Korean language, casual tone, no emoji rules
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Jira draft collection instructions to global CLAUDE.md</name>
  <files>
    groups/global/CLAUDE.md
  </files>
  <read_first>
    groups/global/CLAUDE.md,
    .planning/phases/02-ai-conversation-agent/02-CONTEXT.md,
    .planning/phases/02-ai-conversation-agent/02-RESEARCH.md
  </read_first>
  <action>
    Append a new section to `groups/global/CLAUDE.md` BEFORE the `--- CUSTOM ---` line (before line 117). This section teaches the agent to collect Jira issue information per D-01 through D-08.

    Insert the following block before `--- CUSTOM ---`:

    ```markdown
    ## Jira 이슈 작성 도우미

    사용자가 DevOps 작업 요청이나 버그 리포트를 하면, Jira 이슈 초안을 작성해야 한다.

    ### 수집 필드
    1. **제목** (title): 간결한 한 줄 요약
    2. **설명** (description): 상세 내용. 버그면 재현 방법/기대 동작, 작업이면 구체적 요구사항
    3. **이슈 타입** (issueType): Bug / Task / Story 중 하나

    ### 동작 방식
    1. 사용자 메시지를 분석해서 가능한 필드를 먼저 추론한다
       - "로그인 안 됨" → Bug 추론, 제목 초안 생성
       - "CI 파이프라인 추가해줘" → Task 추론
       - "사용자가 대시보드에서 실시간 알림을 받을 수 있으면 좋겠어" → Story 추론
    2. 이미 파악된 정보는 다시 묻지 않는다
    3. 부족한 정보만 질문한다. 한 번에 하나씩.
    4. 3개 필드가 모두 확보되면 `mcp__nanoclaw__submit_jira_draft` 도구를 호출한다
    5. 도구 호출 후 사용자에게 초안 요약을 짧게 알려준다

    ### 추론 가이드
    - 에러/장애/안 됨/깨짐 → Bug
    - 추가해줘/설정해줘/만들어줘/배포해줘 → Task
    - ~하면 좋겠다/~할 수 있으면 → Story
    - 추론이 확실하지 않으면 사용자에게 물어본다

    ### 톤
    - 바로 본론. 인사/감탄사 불필요
    - 질문은 짧고 직접적으로
    - 예: "이슈 타입은 Bug, Task, Story 중 뭐야?" (O)
    - 예: "안녕하세요! 이슈 타입을 선택해 주시겠어요?" (X)

    ### 주의사항
    - 일반 대화(질문, 잡담, 도움 요청)는 이 워크플로우를 시작하지 않는다. DevOps 작업 요청이나 버그 리포트일 때만 Jira 초안 수집을 시작한다.
    - 사용자가 한 메시지에 모든 정보를 줬으면 바로 `submit_jira_draft`를 호출한다.
    - 제목과 설명은 한국어로 수집한다.
    ```

    Key design decisions implemented:
    - D-01: Inference-first approach — analyze message, infer fields, ask only for missing info
    - D-02: Only 3 fields (title, description, issueType)
    - D-05: All conversation in Korean
    - D-06: Concise, direct tone with examples of good/bad
    - D-07: Auto-submit when all 3 fields collected, no explicit user signal needed
    - D-08: Agent calls submit_jira_draft and terminates the collection flow

    The tool name uses `mcp__nanoclaw__submit_jira_draft` (the full MCP tool name as the agent sees it) to ensure the agent calls the correct tool.
  </action>
  <verify>
    <automated>grep -c 'submit_jira_draft' /Users/spjang/workspace/nanoclaw/groups/global/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - groups/global/CLAUDE.md contains `## Jira 이슈 작성 도우미` heading
    - groups/global/CLAUDE.md contains `mcp__nanoclaw__submit_jira_draft`
    - groups/global/CLAUDE.md contains `Bug / Task / Story` in the 수집 필드 section
    - groups/global/CLAUDE.md contains the inference examples: `"로그인 안 됨"` and `"CI 파이프라인 추가해줘"`
    - groups/global/CLAUDE.md contains tone examples with (O) and (X) markers
    - groups/global/CLAUDE.md still contains `--- CUSTOM ---` section (existing custom rules preserved)
    - The Jira section appears BEFORE `--- CUSTOM ---`
  </acceptance_criteria>
  <done>CLAUDE.md has Jira collection instructions with inference-first strategy, Korean tone guidelines, and submit_jira_draft tool reference. Existing custom rules preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end Jira draft collection in Slack</name>
  <files>groups/global/CLAUDE.md</files>
  <action>
    Build and deploy, then verify the end-to-end flow in Slack:
    1. Run `npm run build && ./container/build.sh` to rebuild with new MCP tool and CLAUDE.md
    2. Restart NanoClaw: `launchctl kickstart -k gui/$(id -u)/com.nanoclaw`
    3. Test in Slack by @mentioning the bot with a work request
  </action>
  <what-built>
    Complete Phase 2 AI conversation agent: IPC actions/ infrastructure (Plan 01) + Jira collection prompt (Plan 02 Task 1). The agent should now conduct multi-turn Korean conversations to collect Jira issue details and submit drafts via IPC.
  </what-built>
  <how-to-verify>
    1. Rebuild the container: `cd /Users/spjang/workspace/nanoclaw && npm run build && ./container/build.sh`
    2. Restart NanoClaw: `launchctl kickstart -k gui/$(id -u)/com.nanoclaw`
    3. In a Slack channel where the bot is registered, send: "@봇이름 로그인 페이지에서 비밀번호 재설정이 안 돼"
    4. Verify: Bot replies IN THE THREAD (not channel) with a question about missing info (should infer Bug type, ask for description details)
    5. Reply in thread with more details (e.g., "Chrome에서 재설정 버튼 클릭하면 404 에러가 뜸")
    6. Verify: Bot calls submit_jira_draft and posts a draft summary in the thread
    7. Check logs: `tail -20 groups/slack_*/logs/container-*.log` for IPC action received
    8. Optional test: Send a one-shot message with all info: "@봇이름 Task: CI에 ESLint 검사 추가해줘. build 단계 후에 lint 단계 넣어야 함" — bot should submit draft immediately without questions
  </how-to-verify>
  <verify>
    <automated>grep -c 'submit_jira_draft' /Users/spjang/workspace/nanoclaw/groups/global/CLAUDE.md</automated>
  </verify>
  <done>Human verified: agent conducts Korean multi-turn conversation, infers fields, calls submit_jira_draft, draft JSON appears in actions/ directory</done>
  <resume-signal>Type "approved" if the conversation flow works, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- groups/global/CLAUDE.md contains Jira collection section with all D-01 through D-08 decisions
- End-to-end: @mention bot -> thread conversation -> draft JSON in actions/ directory
- Agent uses Korean, concise tone, inference-first approach
</verification>

<success_criteria>
- Agent starts thread conversation asking clarifying questions on @mention work request (CONV-01)
- Agent collects title, description, issueType through inference-first Korean dialogue (CONV-02)
- Agent calls submit_jira_draft when all fields collected (feeds into CONV-03 pipeline from Plan 01)
- Human verification confirms end-to-end flow in Slack
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-conversation-agent/02-02-SUMMARY.md`
</output>
